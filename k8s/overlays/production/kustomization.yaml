apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: veltro

bases:
- ../../

commonLabels:
  environment: production

images:
- name: veltro/backend
  newTag: v1.0.0  # Use semantic versioning in production
- name: veltro/frontend
  newTag: v1.0.0

# Production uses base configuration (3 backend replicas, 2 frontend replicas)
