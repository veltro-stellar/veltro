apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: veltro-mainnet

bases:
- ../../

commonLabels:
  environment: mainnet

resources:
- namespace.yaml

patches:
- path: backend-patch.yaml
- path: hpa-patch.yaml
- path: network-policy-patch.yaml
- path: redis-patch.yaml

images:
- name: veltro/backend
  newTag: v1.0.0
- name: veltro/frontend
  newTag: v1.0.0

configMapGenerator:
- name: veltro-config
  behavior: merge
  literals:
  - stellar-network=mainnet
  - rust-log=warn
  - cors-allowed-origins=https://veltro.com
