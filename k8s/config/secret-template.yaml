# This is a template file. DO NOT commit actual secrets to version control.
# Use external secret management (e.g., Sealed Secrets, External Secrets Operator, Vault)
# or create secrets manually using kubectl create secret

apiVersion: v1
kind: Secret
metadata:
  name: veltro-secrets
  namespace: veltro
  labels:
    app: veltro
type: Opaque
stringData:
  # Database URL. The backend is SQLite-only (see
  # docs/adr/0001-sqlite-vs-postgres.md); this must point at a file on
  # the persistent volume mounted into the pod, not a network database.
  # Format: sqlite:///<mount-path>/veltro.db
  database-url: "sqlite:///data/veltro.db"
  
  # Redis URL
  # Format: redis://host:port
  redis-url: "redis://redis:6379"
  
  # JWT Secret (must be at least 32 characters)
  # Generate with: openssl rand -base64 48
  jwt-secret: "CHANGE_ME_generate_with_openssl_rand_base64_48"
  
  # Encryption Key (must be 32-byte hex for AES-256-GCM)
  # Generate with: openssl rand -hex 32
  encryption-key: "CHANGE_ME_generate_with_openssl_rand_hex_32"
  
  # SEP-10 Server Public Key
  sep10-server-public-key: "GXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"
  
  # SEP-10 Home Domain
  sep10-home-domain: "veltro.com"
  
  # Price Feed API Key (optional)
  price-feed-api-key: ""
  
  # Slack Webhook URL (optional)
  slack-webhook-url: ""
  
  # Telegram Bot Token (optional)
  telegram-bot-token: ""

  # AWS credentials for the Litestream sidecar (continuous SQLite
  # backup to S3). On EKS, prefer IRSA (annotate the veltro-backend
  # ServiceAccount with eks.amazonaws.com/role-arn) over static keys --
  # these exist as a portable fallback for non-EKS clusters.
  litestream-aws-access-key-id: "CHANGE_ME"
  litestream-aws-secret-access-key: "CHANGE_ME"

---
# Example: Using Sealed Secrets (recommended for GitOps)
# apiVersion: bitnami.com/v1alpha1
# kind: SealedSecret
# metadata:
#   name: veltro-secrets
#   namespace: veltro
# spec:
#   encryptedData:
#     database-url: AgA...  # Encrypted value
#     redis-url: AgB...     # Encrypted value
#     jwt-secret: AgC...    # Encrypted value

---
# Example: Using External Secrets Operator
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: veltro-secrets
#   namespace: veltro
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: veltro-secrets
#     creationPolicy: Owner
#   data:
#   - secretKey: database-url
#     remoteRef:
#       key: veltro/database-url
#   - secretKey: redis-url
#     remoteRef:
#       key: veltro/redis-url
