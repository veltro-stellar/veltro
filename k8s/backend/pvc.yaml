apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: veltro-backend-data
  namespace: veltro
  labels:
    app: veltro
    component: backend
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
