apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: veltro-backend
  namespace: veltro
  labels:
    app: veltro
    component: backend
    prometheus: kube-prometheus
spec:
  selector:
    matchLabels:
      app: veltro
      component: backend
  endpoints:
  - port: metrics
    interval: 30s
    path: /metrics
    scheme: http
---
apiVersion: v1
kind: Service
metadata:
  name: veltro-backend-metrics
  namespace: veltro
  labels:
    app: veltro
    component: backend
spec:
  type: ClusterIP
  ports:
  - name: metrics
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: veltro
    component: backend
